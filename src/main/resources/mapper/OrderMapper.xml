<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dapang.recharge.mapper.OrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dapang.recharge.pojo.po.OrderPO">
        <result column="user_id" property="userId" />
        <result column="supplier_id" property="supplierId" />
        <result column="product_id" property="productId" />
        <result column="amount" property="amount" />
        <result column="status" property="status" />
        <result column="commission" property="commission" />
        <result column="currency_rate" property="currencyRate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_id, supplier_id, product_id, amount, status, commission, currency_rate
    </sql>

</mapper>
